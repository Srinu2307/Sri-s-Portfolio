services:
  portfolio:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: portfolio_sri
    ports:
      - "5000:5000"
    environment:
      - PORT=5000
      - NODE_ENV=production
    restart: unless-stopped
